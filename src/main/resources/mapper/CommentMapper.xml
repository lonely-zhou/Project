<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="note.recordAndShare.mapper.CommentMapper">
    <select id="selNoteCommentList" resultType="note.recordAndShare.entity.dto.CommentDto">
        select c.*, u.nickname, u.username, u.avatar_url, n.title
        from comment c
                 join user u on u.id = c.user_id
                 join note n on n.id = c.note_id
        where c.note_id = #{noteId}
    </select>
    <select id="selUserCommentList" resultType="note.recordAndShare.entity.dto.CommentDto">
        select c.*, n.title
        from comment c
                 join note n on n.id = c.note_id
        where c.user_id = #{userId}
    </select>
</mapper>
