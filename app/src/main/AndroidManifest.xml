<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.weather">


    <application
        android:allowBackup="true"
        android:icon="@drawable/weather_icon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/weather_icon"
        android:supportsRtl="true"
        android:theme="@style/Theme.Weather">
        <activity
            android:name=".CitySearch"
            android:exported="true" />
        <activity
            android:name=".City"
            android:exported="true" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
    <!-- 权限添加 -->
    <!-- 允许连接网络  android.permission.INTERNET -->
    <uses-permission android:name="android.permission.INTERNET" /> <!-- 访问GPS定位	android.permission.ACCESS_FINE_LOCATION -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" /> <!-- 访问设备状态	android.permission.READ_PHONE_STATE -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" /> <!-- 访问WIFI状态	android.permission.ACCESS_WIFI_STATE -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

</manifest>